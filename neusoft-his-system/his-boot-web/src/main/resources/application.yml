his:
  security:
    jwt-secret: ${HIS_JWT_SECRET:U5vXf9Vx3nQ2rP7mL8sT4wY1aD6kN0hJcB2eR9uM4qZ7tF1pS8yW3vG6xC0nH5j}
    jwt-expire-seconds: ${HIS_JWT_EXPIRE_SECONDS:86400}
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/his_system?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: Ariel830
    driver-class-name: com.mysql.cj.jdbc.Driver
  sql:
    init:
      mode: always # 每次启动时执行 schema.sql
      schema-locations: classpath:schema.sql
mybatis-plus:
  mapper-locations: classpath*:/mapper/**/*.xml
  configuration:
    map-underscore-to-camel-case: true